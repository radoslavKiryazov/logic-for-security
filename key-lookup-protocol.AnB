Protocol: KeyLookup

Types:
  Agent A, I, X;
  Number authTokA;
  Function pk, sk;

Knowledge:
  A: A, I, X, pk(I), authTokA;
  I: A, I, X, pk(I), inv(pk(I)), pk(X);

Actions:
  A -> I: A, X, authTokA
  I -> A: {I, X, pk(X)}inv(pk(I))

Goals:
  pk(X) secret between A,I
  A authenticates I on pk(X)